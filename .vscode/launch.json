{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug cli (train)",
      "program": "${workspaceFolder}/target/debug/cli",
      "args": ["train", "--games", "data"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build cli"
    }
  ]
}
