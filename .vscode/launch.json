{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug cli (add 1 2)",
      "program": "${workspaceFolder}/target/debug/cli",
      "args": ["1", "2"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build cli"
    }
  ]
}
